// Conceptual contrast: blocking loop vs interrupt-driven (pseudocode, verbatim).

void loop()
{
    digitalwrite(Led1, High);
    delay(1000);
    digitalwrite(Led1, Low);
    delay(1000);
}

Or...

interrupt ()
    digitalwrite(led, state);
    state = !state;
}

void loop()
{
}
